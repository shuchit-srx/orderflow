spring:
  application:
    name: api-gateway
  profiles:
    default: local

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

info:
  app:
    name: api-gateway
    version: 0.0.1-SNAPSHOT
